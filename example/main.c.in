/* Dummy main program for dummy.331 language code. */
/* We include dummy.331.h in order to have this file available inside debug tables. */
#include "dummy.331.h"
#include <stdio.h>

extern int our_code_starts_here();

int main(int argc, char** argv) {

  int result = our_code_starts_here();
  printf("%d\n", result);
  return 0;
}
